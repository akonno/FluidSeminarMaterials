# Linux / UNIXコマンドラインの基本

Ubuntuの端末を開き、短いコマンドを実行しながら、コマンドラインの基本に慣れよう。

## shell、terminal、prompt

**terminal（端末）**は文字でコンピューターとやり取りする窓口であり、**shell**は入力されたコマンドを解釈して実行するプログラムである。Ubuntuでは通常Bashを使う。端末の**prompt（プロンプト）**は入力待ちを示し、その後ろにコマンドを入力してEnterを押すと実行され、結果が表示される。

CLIではコマンドを入力して操作し、GUIでは画面上のボタンやアイコンを使う。このページでは端末にコマンドを入力して試す。

## コマンドを実行する

次のコマンドを一つずつ入力して、何が表示されるか確認する。

```bash
date
```

現在の日付や時刻が表示される。

```bash
cal 10 2022
```

カレンダーを表示する。月や年の指定を変えたときの違いも試してみよう。

```bash
cal 2022
cal 10
```

```bash
echo Hello
```

指定した文字列が端末に表示される。

```bash
yes
```

`yes`は同じ文字列を繰り返し出力する。動き続けるコマンドを止めるには、端末でCtrl+Cを押す。

```{admonition} 演習
:class: exercise

- `cal 10 2022`、`cal 2022`、`cal 10`の表示を比べ、指定の違いを説明する。
- 自分の誕生日が何曜日だったか、年を指定して調べる。
- `cal 9 1752`を実行する。この月の表示が特殊なのはなぜか、調べてみる。
- `yes`を実行し、Ctrl+Cで止める。
```

## ファイルとディレクトリ

Linuxでは、ファイルを入れる場所を**directory（ディレクトリ）**と呼ぶ。Windowsの「フォルダー」に相当する。コマンドを実行している場所を**current directory（カレントディレクトリ）**という。

次のコマンドで、現在の場所と、その中にあるファイルやディレクトリを確認できる。

```bash
pwd
ls
```

`pwd`は現在のディレクトリの場所を表示し、`ls`はその中身を一覧表示する。`cd`は作業場所を移動するコマンドである。

```bash
cd ..
pwd
cd ~
pwd
```

`..`は一つ上のディレクトリ、`~`は自分のホームディレクトリを表す。

**absolute path（絶対パス）**は、ルートディレクトリ`/`から始まる場所の表し方である。**relative path（相対パス）**は、カレントディレクトリを基準にした表し方である。同じファイルでも、現在の場所によって相対パスの書き方は変わる。

## 背景：UnixとLinux

LinuxはUnixそのものではないが、Unixに似た考え方を持つOSである。ここで学ぶコマンドラインの基本は、macOSなど他のUnix系環境にも共通する部分が多いが、コマンドや動作がすべて同じとは限らない。詳しい背景は、[WSL導入ページの参考資料](wsl-install.md)を参照できる。

## 次に読むページ

[Windowsとのファイル連携とWSLg](wsl-files-and-gui.md)では、Linux側のファイルをWindowsから開く方法と、GUIアプリの確認方法を扱う。

## ライセンス

```{note}
教材本文は [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/deed.ja)、コード例はMIT Licenseのもとで提供されています。
```
